{
  "name": "student-course-backend",
  "version": "1.0.0",
  "description": "学生选课系统后端 API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "seed": "node src/seed.js",
    "healthcheck": "node -e \"const p = process.env.PORT || 8137; require('http').get('http://127.0.0.1:' + p + '/api/auth/ping', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "express-validator": "^7.2.0",
    "sqlite3": "^5.1.7",
    "sequelize": "^6.37.5",
    "winston": "^3.14.2"
  }
}
